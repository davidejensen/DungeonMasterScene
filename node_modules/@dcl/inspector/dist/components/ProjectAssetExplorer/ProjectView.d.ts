/// <reference types="react" />
import { AssetNode, AssetNodeFolder } from './types';
type Props = {
    onImportAsset(): void;
    folders: AssetNodeFolder[];
};
export declare const ROOT = "File System";
export type TreeNode = Omit<AssetNode, 'children'> & {
    children?: string[];
};
declare function ProjectView({ folders, onImportAsset }: Props): JSX.Element | null;
export default ProjectView;
