import { FileSystemInterface } from '../types';
export declare function getFilesInDirectory(fileSystem: FileSystemInterface, dirPath: string, files: string[], recursive?: boolean, ignore?: string[]): Promise<string[]>;
