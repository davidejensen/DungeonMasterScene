import { Scene } from '@babylonjs/core';
import { Layout } from '../../utils/layout';
export declare const getLayoutManager: (a: Scene, ...params: never[]) => {
    getLayout: () => Layout;
    setLayout: (_layout: Layout) => void;
};
