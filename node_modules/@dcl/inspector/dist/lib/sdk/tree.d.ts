import { Entity, IEngine } from '@dcl/ecs';
import { EditorComponents } from './components';
import { Operations } from './operations';
export declare const ROOT: Entity;
/**
 * Returns a tree of in the shape of Map<Entity, Set<Entity>> where the key is the parent and the value is the children
 * @returns
 */
export declare const getTreeFromEngine: (engine: IEngine, operations: Operations, EntityNode: EditorComponents['EntityNode']) => Map<Entity, Set<Entity>>;
/**
 * Used to return an empty tree when the sdk is not ready
 * @returns
 */
export declare function getEmptyTree(): Map<Entity, Set<Entity>>;
